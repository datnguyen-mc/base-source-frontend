/**
 * Layout Template
 * This is the default layout component.
 * Should use this layout as the wrapper for all generated pages.
 * 
 * Usage for AI:
 * - Wrap page content inside <Layout currentPageName="...">{children}</Layout>
 * - Do not duplicate header/footer in new pages.
 * - Use the existing navigation and structure defined here.
 */
